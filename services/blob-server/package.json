{
  "name": "@pagex/blob-server",
  "version": "1.2.0",
  "description": "PageX Blob Server - Caddy with static_s3 plugin for content-addressed blob serving",
  "private": true,
  "scripts": {
    "build": "CGO_ENABLED=0 go build -tags=nobadger,nomysql,nopgx -ldflags='-s -w' -trimpath -o caddy ./cmd/caddy",
    "test": "go test -v ./src/...",
    "run": "go run ./cmd/caddy",
    "xcaddy": "echo 'deprecated: use pnpm build (go build ./cmd/caddy)' && pnpm run build"
  },
  "keywords": [
    "pagex",
    "blob-server",
    "caddy",
    "static-s3",
    "storage",
    "s3"
  ],
  "author": "",
  "license": "MIT"
}